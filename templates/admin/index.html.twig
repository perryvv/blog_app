{% extends 'base.html.twig' %}

{% block title %}Post index{% endblock %}

{% block body %}
    <h1>Admin Panel</h1>
    <a href="{{ path('post_index') }}"><button class="btn btn-primary"> Terug naar blog </button></a>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titel</th>
                <th>Subtitel</th>
                <th>Publishdate</th>
                <th>Img_url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr>
                <td>{{ post.id }}</td>
                <td>{{ post.titel }}</td>
                <td>{{ post.subtitel }}</td>
                <td>{{ post.publishdate ? post.publishdate|date('Y-m-d') : '' }}</td>
                <td>{{ post.imgUrl }}</td>
                <td>
                    <a href="{{ path('admin_show', {'id': post.id}) }}"><button class="btn btn-primary">show</button></a>
                    <a href="{{ path('admin_edit', {'id': post.id}) }}"><button class="btn btn-warning">edit</button></a>
                    {{ include('admin/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">Geen blogposts gevonden.</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('admin_new') }}"><button class="btn btn-success">Create New</button></a>
{% endblock %}
