<form method="post" action="{{ path('admin_delete', {'id': post.id}) }}" onsubmit="return confirm('Zeker weten?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
